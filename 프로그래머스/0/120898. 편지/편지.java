class Solution {
    public int solution(String message) {
        int answer = 0;
        
        int m_length = message.length();
        answer = m_length * 2;
        
        return answer;
    }
}